apiVersion: v1
kind: Service
metadata:
  name: quote
  labels:
    app.kubernetes.io/name: {{ include "quote-service.name" . }}
    app.kubernetes.io/component: quote
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: tcp-service
      targetPort: service
  selector:
    app.kubernetes.io/component: quote
