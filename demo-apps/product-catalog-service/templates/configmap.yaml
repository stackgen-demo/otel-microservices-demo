apiVersion: v1
kind: ConfigMap
metadata:
  name: product-catalog-products
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "product-catalog-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
data:
  # Product catalog data will be mounted here
  # This is a placeholder - actual product data should be added
  .gitkeep: ""

