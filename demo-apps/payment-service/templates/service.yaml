apiVersion: v1
kind: Service
metadata:
  name: payment
  labels:
    app.kubernetes.io/name: {{ include "payment-service.name" . }}
    app.kubernetes.io/component: payment
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: tcp-service
      targetPort: service
  selector:
    app.kubernetes.io/component: payment
