apiVersion: v1
kind: Service
metadata:
  name: shipping
  labels:
    app.kubernetes.io/name: {{ include "shipping-service.name" . }}
    app.kubernetes.io/component: shipping
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: tcp-service
      targetPort: service
  selector:
    app.kubernetes.io/component: shipping
