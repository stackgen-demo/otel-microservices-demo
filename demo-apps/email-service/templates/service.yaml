apiVersion: v1
kind: Service
metadata:
  name: email
  labels:
    app.kubernetes.io/name: {{ include "email-service.name" . }}
    app.kubernetes.io/component: email
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: tcp-service
      targetPort: service
  selector:
    app.kubernetes.io/component: email
