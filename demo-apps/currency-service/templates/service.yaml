apiVersion: v1
kind: Service
metadata:
  name: currency
  labels:
    app.kubernetes.io/name: {{ include "currency-service.name" . }}
    app.kubernetes.io/component: currency
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: tcp-service
      targetPort: service
  selector:
    app.kubernetes.io/component: currency
